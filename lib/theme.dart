import 'package:flutter/material.dart';

// ═══════════════════════════════════════════════════════════════════════
// Palette — the foosball table under a bar light. Matches the web app's
// colors exactly so the native chrome (update banner) blends into the page.
// ═══════════════════════════════════════════════════════════════════════

const Color kBg = Color(0xFF0B0E0C); // ground
const Color kCard = Color(0xFF121714); // panel
const Color kInset = Color(0xFF1A211C);
const Color kBorder = Color(0xFF2A332C);
const Color kInk = Color(0xFFE8EDE7);
const Color kMuted = Color(0xFF8C9A8F);
const Color kAccent = Color(0xFFF2E9C8); // the ball — cream
const Color kWarn = Color(0xFFE23A2E); // Kevin red / error
